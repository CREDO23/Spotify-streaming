/** @format */

const logginTypes = {
	SET_LOGIN_USER: 'SET_LOGIN_USER',
};

const tokenTypes = {
	SET_ACCESS_tOKEN: 'SET_ACCES_TOKEN',
};

export default tokenTypes;

// module.exports = { logginTypes, tokenTypes };
